package com.example.profilemanager.eventdriven;

import lombok.Value;
/*
public class ProfileEventPublisher {
    // conig amqp, and what exchange iam i puplishing to?

    private final AmqpTemplate amqpTemplate;
    private final String exchangeName;

    public ProfileEventPublisher(
            final AmqpTemplate amqpTemplate;
            @Value("${}") final String exchangeName
    ){
        this.amqpTemplate = amqpTemplate;
        this.exchangeName = exchangeName;
    }
    public void publishProfileEventString(

    ){
        // Build the message

        // decide the routing

        // send the ting
    }
}
*/
